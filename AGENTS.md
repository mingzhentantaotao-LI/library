# AGENTS.md

## 项目目标

这是一个面向个人长期使用的 Markdown 知识库。V1 只处理三类内容：

1. 工作内容
2. 学习笔记
3. 收藏的学习材料

维护目标：

- 只做可持续维护的 Markdown 知识整理。
- `raw/` 保存原始资料，作为证据层和回溯层。
- `wiki/` 保存整理后的知识、结论、索引和评审。
- 优先保证可读性、可追溯性和增量维护成本低。
- 不做前端，不做数据库，不做复杂 RAG 平台。

## 新会话启动规则

当新的会话、project 或维护者接手本仓库时，先阅读 `PROJECT_HANDOFF.md` 获取当前阶段上下文，再阅读 `AGENTS.md`、`README.md`、`wiki/index.md` 和 `wiki/log.md`。

`PROJECT_HANDOFF.md` 只用于交接上下文，不覆盖本文件中的长期维护规则。

## 目录职责

### `raw/`

原始资料层，只读。

- `raw/inbox/`: 新材料进入知识库的唯一默认入口。
- `raw/work/`: 工作相关原始资料的长期归档区。
- `raw/study/`: 学习笔记、课程、教材等原始资料归档区。
- `raw/clips/`: 收藏文章、网页摘录、零散材料的原始归档区。
- `raw/assets/`: 原始图片、附件、截图、PDF 等素材。
- `raw/templates/`: 工作材料捕获模板，用于后续人工生成真实材料；模板本身不是待 ingest 的来源。

### `wiki/`

整理后的知识层，由维护者持续更新。

- `wiki/index.md`: 总入口与导航页。
- `wiki/log.md`: 追加式变更日志。
- `wiki/overview.md`: 知识库范围、规则与当前结构概览。
- `wiki/projects/`: 具体项目、事项、任务脉络。
- `wiki/topics/`: 主题型知识整理。
- `wiki/concepts/`: 概念解释与定义。
- `wiki/methods/`: 方法、流程、模版、实践套路。
- `wiki/decisions/`: 决策记录、取舍和结论。
- `wiki/sources/`: 原始资料的索引、摘要和出处登记。
- `wiki/reviews/`: 对材料、项目、阶段性结论的复盘与评审。

### `scripts/`

辅助脚本目录。V1 允许保留目录，但不预设复杂实现。

- `scripts/ingest/`: 资料摄取辅助脚本。
- `scripts/lint/`: 链接、结构、命名等检查脚本。
- `scripts/export/`: 导出与打包辅助脚本。
- `scripts/utils/`: 通用小工具。

### `.codex/`

项目级代理配置和协作约束。

## `raw/` 只读规则

- 代理只能读取 `raw/`，不能修改、覆盖、重命名、删除其中任何原始文件。
- 禁止在 `raw/` 内对原始资料做清洗、改写、摘要回填或格式重写。
- 对原始资料的任何整理，都必须写入 `wiki/`，而不是回写到 `raw/`。
- 若需要对原始资料分类、摘录、纠错或补充上下文，必须新建或更新对应 `wiki/` 页面，并引用原始路径。
- `raw/inbox/` 中的新文件默认视为未处理来源；是否归档到 `raw/work/`、`raw/study/`、`raw/clips/`，由人工决定，不由代理自动搬运。

## Ingest 工作流

当有新材料进入知识库时，按以下顺序处理：

1. 用户先把材料放入 `raw/inbox/`。
2. 代理只读取材料，不改动原文件。
3. 在 `wiki/sources/` 新建或更新对应来源页，记录：
   - 原始文件路径
   - 材料类型
   - 时间信息
   - 核心主题
   - 简要摘要
   - 后续可关联的项目/主题/概念
4. 如材料值得沉淀，继续在 `wiki/projects/`、`wiki/topics/`、`wiki/concepts/`、`wiki/methods/`、`wiki/decisions/` 或 `wiki/reviews/` 中生成整理页。
5. 更新 `wiki/index.md` 中的相关入口。
6. 在 `wiki/log.md` 追加一条处理记录。

## `wiki/sources/` 最小模板规范

每个新建来源页至少应包含以下结构：

- 标题行：`# 来源：...`
- `## 基本信息`
  - `标题`
  - `来源类型`
  - `日期`
  - `原始路径`
- `## 简要摘要`
  - 使用 1 段简短摘要说明这份材料为什么值得保留
- `## 关键点`
  - 默认至少整理 3 条要点
- `## 关联页面`
  - 链接本轮实际更新或确认相关的页面

补充规则：

- 日期优先写材料自身日期；若原文没有明确日期，可写本次记录日期，并标注为“记录日期”。
- `原始路径` 必须指向 `raw/` 中对应文件的相对路径。
- 来源页只做来源登记、摘要和回链，不在来源页中展开大段二次论述。

## `raw/inbox/` 与 `wiki/sources/` 命名映射规则

- 默认采用“一份原始材料，对应一个来源页”的映射。
- `wiki/sources/` 页面文件名默认与 `raw/inbox/` 原文件保持同一 basename，只把扩展名改为 `.md`。
- 示例：
  - `raw/inbox/2026-04-23-study-persistent-wiki-middle-layer-notes.md`
  - `wiki/sources/2026-04-23-study-persistent-wiki-middle-layer-notes.md`
- 若原始材料是 `.html`、`.pdf`、`.txt` 等非 Markdown 文件，来源页仍使用相同 basename，并统一写成 `.md`。
- 同一轮 ingest 中，不要为同一份 `raw/inbox/` 材料额外创建第二个不同命名的来源页。

## 单轮 Ingest 页面上限与优先级

- 单轮 ingest 必须先完成对应 `wiki/sources/` 来源页。
- 在来源页之外，默认最多只更新 2 个知识页：
  - 最多 1 个 `project page`
  - 以及最多 1 个 `topic / concept / method / decision / review` 页面
- `wiki/index.md` 与 `wiki/log.md` 属于配套更新，不计入上述页面上限。
- 优先级顺序为：
  1. 建立或补全来源页
  2. 优先更新已有页面，而不是新开页面
  3. 只有当材料已经形成稳定结论时，才提升到更高层页面
- 若本轮材料仍然偏初步、存在冲突或暂时不值得提升，则停在来源页、`wiki/index.md` 和 `wiki/log.md`，不要继续扩展页面。

## Query 工作流

当用户基于知识库提问或要求整理时，按以下顺序处理：

1. 优先检索 `wiki/` 中已有整理结果。
2. 若 `wiki/` 中结论不足，再回读 `raw/` 相关材料补证据。
3. 输出答案时，优先引用 `wiki/` 页面；必要时附上对应 `raw/` 路径作为来源。
4. 若提问形成了新的稳定结论或更好的结构化整理，回写到 `wiki/`。
5. 若结论存在冲突，明确标注来源差异、时间差异和不确定点，不做无依据合并。

## Review 工作流

V1 的 review 只做轻量规则复查与结构判断，不做自动评分，也不要求每次都回写大量页面。

### 触发时机

以下任一条件满足时，应做一次 review：

1. 手动触发：当用户明确要求“复盘”“review”“做一轮检查”。
2. 周期触发：知识库处于活跃维护阶段时，建议每周做一次轻量 review。
3. 节点触发：每累计完成 2 次真实 ingest 后，建议做一次 review。
4. 异常触发：当出现跨主题材料难以归类、来源冲突、命名漂移或索引遗漏迹象时，可提前做 review。

### 最小检查清单

每次 review 优先检查以下问题：

1. 是否存在 `wiki/sources/` 页面只有来源登记，但没有被有效提升或关联到合适的项目页、主题页或其他知识页。
2. 是否存在 `wiki/projects/` 或 `wiki/topics/` 页面长期未更新，但最近新增来源已经明显与其相关。
3. 是否存在跨主题材料被勉强塞进某个已有页面，导致页面主题边界变得模糊。
4. 是否存在命名漂移、来源缺失、`wiki/index.md` 入口遗漏、回链不足等结构问题。
5. 是否有重复出现且已经稳定的结论，值得进一步沉淀为 `wiki/methods/` 或 `wiki/decisions/`。

### 输出形式

- 每次 review 的最小输出应是 `wiki/reviews/` 下的一份 Markdown 记录。
- 文件名建议采用：`YYYY-MM-DD-review-<scope>.md`
- scope 可以是 `weekly-maintenance`、`ingest-check`、`topic-alignment` 这类简短语义。
- review 输出应以“发现 + 建议动作”为主，不做复杂分数、等级或图表。
- 默认使用 `wiki/reviews/review-template.md` 作为起点，再按本次 review 范围做最小修改。

### 是否回写 wiki

- review 本身应先产出 review 记录，再决定是否发起后续整理。
- 若 review 只发现轻微问题，可先记录在 `wiki/reviews/`，暂不立即改动其他页面。
- 若 review 发现明确且小范围的问题，例如索引遗漏、来源缺失、明显命名漂移，可在单独的一轮小改动中回写 `wiki/`。
- 若 review 发现适合沉淀为 `method` 或 `decision` 的稳定内容，应在后续单独整理，而不是在 review 记录里直接扩写过多长期知识。
- 每次新增 review 记录后，应同步更新 `wiki/index.md`，并在 `wiki/log.md` 追加一条简短记录。

## Lint 工作流

V1 的 lint 以轻量规则为主，重点检查结构质量，而不是复杂自动化。

检查顺序：

1. 目录和文件命名是否清晰、稳定、可复用。
2. `wiki/` 页面是否存在死链、断链或缺失入口。
3. 页面是否标明来源或至少能回链到 `wiki/sources/`。
4. 重要结论是否能定位到原始资料路径或来源页。
5. `wiki/index.md` 是否覆盖新增页面入口。
6. `wiki/log.md` 是否记录了本次增量变更。

## `wiki/index.md` 更新规则

- 新增 `wiki/` 页面后，必须为其补充至少一个入口。
- 优先更新 `wiki/index.md`，必要时同步更新相关主题页或项目页中的导航区。
- `wiki/index.md` 只保留稳定入口，不堆积零散临时链接。
- 相同页面不要重复挂载多个完全等价的入口；应选择最合理的归类位置。

## `wiki/log.md` 追加规则

- `wiki/log.md` 采用追加写，不覆盖旧记录。
- 每次有新增页面、重要更新、结构调整或结论修订，都要追加一条记录。
- 记录至少包含：
  - 日期
  - 动作类型
  - 涉及页面
  - 简短说明
- 禁止为了“整理美观”而删除历史日志。

## 引用与回链规则

- 每个重要的 `wiki/` 页面都应尽量包含“来源”或“参考”区块。
- 来源优先指向 `wiki/sources/` 页面；必要时附原始 `raw/` 路径。
- 整理页之间应使用相对链接建立回链，例如：
  - 项目页链接到相关主题页
  - 主题页链接到概念页
  - 决策页链接到对应来源页和项目页
- 若一个结论来自多个来源，应并列列出，不做隐式合并。
- 若后续发现旧结论需要修正，应保留历史说法，并新增修订说明与来源，而不是直接抹去原判断。

## 禁止事项

- 不要修改 `raw/` 中的原始文件。
- 不要删除历史结论；如需修订，使用“补充说明”“修订记录”或“新结论覆盖旧结论并保留引用”的方式处理。
- 不要在无来源的情况下生成确定性事实。
- 不要把临时讨论直接当作稳定知识写入 `wiki/`。
- 遇到来源冲突时，不要强行统一；必须标注冲突来源、时间和上下文差异。
- 不要引入前端、数据库或复杂 RAG 方案作为 V1 默认实现。

## 2026-04-28 方向修订：薄 Web 层

用户已明确改变方向：当前阶段允许先实现一个相对简单的网页，用于查询、上传、预览和删除文件；数据库继续延后。

新的边界：

- 可以新增 `web/` 薄应用层，但不能让它取代 `raw/ + wiki/` 的文件系统事实来源。
- 上传默认进入 `raw/inbox/`，也可以进入 `raw/work/`、`raw/study/`、`raw/clips/`、`raw/assets/`。
- 删除必须优先采用安全删除，即移动到 `.trash/`，不要直接物理删除。
- AI 文件管理接入应先提供分类建议、来源页草稿和关联页面建议，不默认自动改写稳定知识页。
- 数据库、复杂全文索引、复杂 RAG 和权限系统仍然不是当前阶段默认实现。

这条修订覆盖上文“不要引入前端作为 V1 默认实现”的旧边界，但不覆盖 `wiki/` 可追溯、低维护成本和小步整理的长期原则。
